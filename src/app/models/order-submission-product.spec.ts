import { OrderSubmissionProduct } from './order-submission-product';

describe('OrderSubmissionProduct', () => {
  it('should create an instance', () => {
    expect(new OrderSubmissionProduct()).toBeTruthy();
  });
});
