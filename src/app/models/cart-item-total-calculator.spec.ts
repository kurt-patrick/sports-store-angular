import { CartItemTotalCalculator } from './cart-item-total-calculator';

describe('CartItemTotalCalculator', () => {
  it('should create an instance', () => {
    expect(new CartItemTotalCalculator()).toBeTruthy();
  });
});
